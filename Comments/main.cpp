#include <iostream>

using namespace std;

int main()
{
    // already found this option. The one downside to this form of comment is that when new lines are created you have to write it again.
    /* everything that comes afterwards will be a comment unless it is closed by */
    cout << "Hello world!" << endl;

    return 0;
}
