#include <iostream>

using namespace std;

int main()
{
    char grade = 'A';       //char variable stores a single character. The character has to be defined with a single quation mark '' else it will raise a conversion error
    string phrase = "Asser badjan";
    int age = 50;
    double GPA = 3.6;    //there are two datatypes for storing decimals. float and double. double can store more decimal points than floats
    bool isMale = true;      //true is not capitalized in c++

    cout << grade << endl;

    return 0;
}
